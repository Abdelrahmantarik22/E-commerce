import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
    images: {
    domains: ["ecommerce.routemisr.com","avatars.githubusercontent.com"]
  }
};

export default nextConfig;
